*~
.*.swp
.swp
*.log
.DS_Store
.vscode/
.direnv/
.envrc.local
.flatpak-builder/
zig-cache/
.zig-cache/
# …and as a symlink onto another disk, which a trailing-slash rule misses.
.zig-cache
zig-out/
/result*
/.nixos-test-history
example/*.wasm
test/ghostty
test/cases/**/*.actual.png
flatpak/builddir/
flatpak/repo/

glad.zip
/Box_test.ppm
/Box_test_diff.ppm
/ghostty.qcow2

vgcore.*
trm-*.zip

# Investigation sample dumps (large, regenerable with `sample <pid>`)
.trm-debug/*.txt

# iOS app build output
ios/build/

# Local xcodebuild derived data (macos/build is a machine-specific symlink)
macos/build
macos/build-release/
ios/build-ios/

# Artifacts the zig sprite test writes relative to its working directory
sprite_face_test-*.png
issues/artifacts/

# Wrangler's local state for the website worker
website/.wrangler/

# Python bytecode from the helper scripts
scripts/__pycache__/
